[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eval-harness"
version = "0.1.0"
description = "Experimental LLM evaluation harness over a golden dataset."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
